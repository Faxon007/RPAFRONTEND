import { InitialsLettersPipe } from './initials-letters.pipe'

describe('InitialsLettersPipe', () => {
	it('create an instance', () => {
		const pipe = new InitialsLettersPipe()
		expect(pipe).toBeTruthy()
	})
})
