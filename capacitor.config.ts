import { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
	appId: 'io.ionic.starter',
	appName: 'claro-oym-portal-frontend',
	webDir: 'www',
	bundledWebRuntime: false,
}

export default config
